{
  "name": "pepper-coast",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "npx serve public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
